#include "main.h"
/**
 * _abs - computes the absolute value of a num
 *
 *@n: the num to print
 *
 *Return: gives absolute value
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}


